<% if ( locals.canonicalURL ) { %>
    <link rel="canonical" href="<%= locals.canonicalURL; %>" />
<% } else if ( locals.isBase ) { %>
    <link rel="canonical" href="<%= env.APP_PUBLIC_BASE_URL; %>" />
<% } %>
